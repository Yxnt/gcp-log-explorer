1. 如果有交互式的 shell 不要执行等待用户执行完成后来获取反馈结果然后再进行下一步
2. 每个任务开始前先检查 Linear 中是否有任务，如果没有的话先创建任务
3. 每个任务完成后更新记录到 Linear 任务中
4. 每个任务确保任务记录已经更新到Linear中后再更新任务状态到 Linear 中
5. 每个任务开始前先将任务状态更新为 WIP
6. 每个任务都关联到linear中的 project "GCP Log Explorer GUI"
